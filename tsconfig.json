{
  "compilerOptions": {
    /* 基础路径映射 */
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    },

    /* 通用编译选项 */
    "target": "ESNext",
    "lib": ["ESNext", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx",

    /* 模块系统（支持 import.meta & Node & Vite） */
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "resolveJsonModule": true,
    "esModuleInterop": true,
    "allowJs": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,

    /* 仅做类型检查，不生成 JS（交给 Vite/Node 运行时处理） */
    "noEmit": true,
    "isolatedModules": true,
    "allowImportingTsExtensions": true,

    /* 类型检查松紧度 */
    "strict": false,
    "noImplicitAny": false,
    "noUnusedLocals": false,
    "noUnusedParameters": false,
    "strictNullChecks": false,

    /* Vite 相关 */
    "types": ["vite/client"]
  },
  "include": [
    "src",
    "api",                // Node/后端 API 代码
    "vite.config.ts",
    "vite-env.d.ts"
  ],
  "exclude": [
    "node_modules",
    "dist"
  ]
}
